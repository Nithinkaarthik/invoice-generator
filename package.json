{
  "name": "invoice-generator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm start"
  },
  "engines": {
    "node": ">=18.x"
  }
}